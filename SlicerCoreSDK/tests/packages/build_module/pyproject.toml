[build-system]
requires = ["scikit-build-core", "slicer-core-sdk", "vtk-sdk"]
build-backend = "scikit_build_core.build"

[tool.scikit-build]
build-dir = "wheelbuild"

[project]
name = "slicer-dummy"
version = "1.0.0"
authors = [
  { name = "Alexy Pellegrini", email = "alexy.pellegrini@kitware.com" }
]
dependencies = ["slicer-core"]
