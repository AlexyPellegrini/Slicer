[build-system]
requires = ["scikit-build-core", "vtk-sdk==9.5.0"]
build-backend = "scikit_build_core.build"

[tool.scikit-build]
build-dir = "wheelbuild"

[project]
name = "slicer-core"
version = "5.10.0"
dependencies = ["vtk==9.5.0"]
